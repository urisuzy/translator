# OpenAI-compatible endpoint used as the primary translator.
LLM_BASE_URL=
LLM_API_KEY=
LLM_MODEL=translate-flash

# Fallbacks only. Proxy: MyMemory counts its free quota per exit IP.
TRANSLATE_PROXY=
MYMEMORY_EMAIL=
